spring:
  datasource:
    url: jdbc:mariadb://172.168.30.50:3306/bp
    username: bpuser
    password: ENC(WnCvqyx1nX8YZ2WbTNCc9Q==)
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDBDialect
    show-sql: true

# prod 프로필에서만MariaDB + 암호문 사용
